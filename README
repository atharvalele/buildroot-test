The tarball must contain:

 - status, with the text "OK", "NOK" or "TIMEOUT", which indicates
   whether the build was successful (OK), failed (NOK) or timed-out
   (TIMEOUT).

 - gitid, which should contain the Git commit id indicating the state
   of the Buildroot repository which was used for the test

 - build-end.log, the 100 last lines of the build log, uncompressed

 - config, the Buildroot configuration file that was used

 - defconfig, the Buildroot defconfig file that was used

 - submitter, contains the name of the build submitter
